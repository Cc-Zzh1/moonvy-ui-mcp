{
  "name": "moonvy-ui-mcp",
  "version": "0.1.1",
  "description": "Read Moonvy design metadata, layers, UI parameters, design tokens, and assets through MCP.",
  "type": "module",
  "bin": {
    "moonvy-ui-mcp": "src/server.js"
  },
  "files": [
    "src",
    "README.md",
    ".env.example",
    "LICENSE"
  ],
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/moonvy-client.js && node --check src/genome.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "dotenv": "^17.2.3",
    "zod": "^4.4.3"
  },
  "license": "MIT"
}
